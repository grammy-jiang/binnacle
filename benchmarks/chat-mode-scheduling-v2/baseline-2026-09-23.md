# Chat mode scheduling v2 — Step 0.1 baseline freeze

Status: frozen on 2026-09-23T10:45:56+10:00.

## A-arm definition

The benchmark A arm is the **actual live** `rp-test-sandbox` Project state,
not the repository's legacy MCP instruction file.

- Project: `rp-test-sandbox`
- Project id: `g-p-6aaea9da2bc881918d6f9eb5177cf904`
- live instruction SHA-256: `f1c100d0ddb93c29f8f78e5a2d28e297d861ba6e2c7e53712ec886dea06bafa5`
- legacy repo instruction matches live: **False**
- proposed v2 instruction matches live: **False**

Step 0.3 corrected this hash to the exact backend instruction bytes. The original Step-0.1 CLI stdout included one extra newline added by `print()`; that original capture remains in the JSON for provenance.

Exact live instructions:

```text
rp-test instructions v2: sandbox for the chatgpt-web-operations skill tests. Reply in one short sentence.
```

## Chat model baseline

A real new chat created inside the benchmark Project reported:

- `model_slug=gpt-5-6-thinking`
- `resolved_model_slug=gpt-5-6-thinking`
- `thinking_effort=max`

The probe conversation is test-only and is deleted after this snapshot is
verified.

## Production MCP baseline

- production commit: `83862b040f59afc97963041de557bf9037f13e50`
- service: `active/running`
- connector: six normal ChatGPT tools
- `run_command.wait_max_s=50`
- `jobs.warmup_s=1.0`
- `jobs.quiet_after_s=30`
- `blocking_wait_budget_s_by_client={}`

A/B work must not mutate this production server while testing instruction-only
Phase 1 variants.

## Host baseline

- host: `raspberrypi`
- kernel: `6.18.50+rpt-rpi-2712`
- arch: `aarch64`
- Python: `Python 3.13.5`

Machine-readable source: `benchmarks/chat-mode-scheduling-v2/baseline-2026-09-23.json`.
