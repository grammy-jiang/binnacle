{
  "benchmark_class": "macro",
  "dag": [
    {
      "access": "non_read_only",
      "arguments": {
        "background": true,
        "command": "python3 -c 'import time; time.sleep(60); print(\"R7-A-{nonce}\")'",
        "wait_seconds": 1,
        "workdir": "{root}"
      },
      "depends_on": [],
      "id": "start_first",
      "kind": "tool",
      "tool": "run_command"
    },
    {
      "access": "read_only",
      "allow_repeats": true,
      "arguments": {
        "job_id": "{result:start_first.job_id}",
        "tail_lines": 20
      },
      "completion_condition": "job_state=exited",
      "depends_on": [
        "start_first"
      ],
      "id": "wait_first",
      "kind": "tool",
      "tool": "job_status"
    },
    {
      "access": "non_read_only",
      "arguments": {
        "background": true,
        "command": "python3 -c 'import time; time.sleep(75); print(\"R7-B-{nonce}\")'",
        "wait_seconds": 1,
        "workdir": "{root}"
      },
      "depends_on": [
        "wait_first"
      ],
      "id": "start_second",
      "kind": "tool",
      "tool": "run_command"
    },
    {
      "access": "read_only",
      "allow_repeats": true,
      "arguments": {
        "job_id": "{result:start_second.job_id}",
        "tail_lines": 20
      },
      "completion_condition": "job_state=exited",
      "depends_on": [
        "start_second"
      ],
      "id": "wait_second",
      "kind": "tool",
      "tool": "job_status"
    }
  ],
  "expected_runtime_s": 165,
  "fixture": {
    "allowed_mutation_globs": [
      "{root}/**"
    ],
    "files": [],
    "jobs": [],
    "kind": "ephemeral_dir",
    "production_mutation_allowed": false,
    "root_template": "/tmp/binnacle-chat-scheduling-v2/{id}/{run_id}"
  },
  "id": "R7",
  "max_turn_runtime_s": 230,
  "metric_tags": [
    "wall_time",
    "repeated_dependency_wait",
    "same_prompt_completion",
    "continuation_prompts"
  ],
  "oracle": {
    "checks": [
      {
        "params": {},
        "type": "all_nodes_complete"
      },
      {
        "params": {
          "exit_code": 0,
          "node": "wait_first",
          "state": "exited"
        },
        "type": "job_state"
      },
      {
        "params": {
          "exit_code": 0,
          "node": "wait_second",
          "state": "exited"
        },
        "type": "job_state"
      },
      {
        "params": {
          "value": "DONE"
        },
        "type": "exact_reply"
      }
    ],
    "terminal_state": "all_required_nodes_complete"
  },
  "phase1_step": "1.8",
  "prompt_template": "Use Raspberry Pi MCP and complete two required jobs sequentially in this prompt. First, in {root}, start this exact command with background=true and wait_seconds=1: python3 -c 'import time; time.sleep(60); print(\"R7-A-{nonce}\")'. Wait until it succeeds. Only then start this exact second command in {root} with background=true and wait_seconds=1: python3 -c 'import time; time.sleep(75); print(\"R7-B-{nonce}\")'. Wait until it succeeds. Reply only DONE after both complete.",
  "purpose": "Stress legitimate repeated waits and same-prompt completion without useful side work.",
  "schema_version": 1,
  "title": "Two sequential long dependency barriers"
}
